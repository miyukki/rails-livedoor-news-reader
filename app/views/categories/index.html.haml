- @categories.each do |category|
  = link_to category.name, category_path(category)
  - category.articles.recent.first(5).each do |article|
    .article
      = link_to article.title, article_path(article)
